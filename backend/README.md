# backend/

This folder will hold the Laravel REST API application serving
api.trigonapex.in and MySQL as its database.

**Status: empty placeholder.** Scaffolding
(`composer create-project laravel/laravel`) has not been run yet — see
`PROJECT_SETUP.md` at the repository root for the planned setup sequence and
required approvals before this stage begins.
