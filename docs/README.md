# docs/

Project documentation that expands as the project progresses. The two
foundational documents currently live at the repository root for visibility:

- `../ARCHITECTURE.md` — system architecture and technical decisions
- `../PROJECT_SETUP.md` — setup sequence and open decisions

Documents written so far:

- `DATABASE_ARCHITECTURE.md` — MySQL schema foundation (Stage 5)
- `CONTENT_ARCHITECTURE.md` — website content structure (Stage 6)

Still to come, as their stages are reached:

- `API.md` — REST API contract (expanded as business endpoints are added)
- `DEPLOYMENT.md` — step-by-step production deployment guide
- `CONTRIBUTING.md` — branching, commit, and PR conventions (added when a
  team workflow is needed)
